# Encatch: set API key and feedback form ID on the Encatch Test page (stored in localStorage).
# Optional Vite dev proxies — match the environment selected in Encatch Test:
# ENCATCH_PROXY_API_TARGET=https://api.dev.encatch.com
# ENCATCH_PROXY_WEB_TARGET=https://form.dev.encatch.com
